# Safari Adapter

Classification: `supported system inheritance only`

Safari does not expose a Chrome-style browser chrome theme target for this tool.

The Safari adapter performs no writes. Safari may still reflect system-level appearance changes such as light/dark mode and system accent color.

Apply:

```bash
macwal apply --targets safari
```

This command succeeds as an informational no-op.
